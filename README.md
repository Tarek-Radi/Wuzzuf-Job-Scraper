# Wuzzuf-Job-Scraper
This project is a web scraping script that extracts job listings from Wuzzuf based on a search query (e.g., Data Engineer). It collects structured job data across multiple pages and exports the results into a CSV file.
